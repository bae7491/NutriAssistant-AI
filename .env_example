# ===========================================
# 환경 설정 (local 또는 aws)
# ===========================================
APP_ENV=local

# ===========================================
# 서버 설정
# ===========================================
# 로컬 환경
LOCAL_HOST=127.0.0.1
LOCAL_PORT=8000

# AWS 환경 (아래 값을 실제 AWS 설정으로 수정하세요)
AWS_HOST=0.0.0.0
AWS_PORT=8000
AWS_APP_DIR=/home/ec2-user/ai-app

# ===========================================
# API URL 설정 (Spring Boot 등 외부 서비스 연동 시 사용)
# ===========================================
# 로컬 Spring Boot URL (예시 - 실제 주소로 수정하세요)
LOCAL_SPRING_URL=http://localhost:8080

# AWS Spring Boot URL (예시 - 실제 AWS 주소로 수정하세요)
# 형식: http://[EC2-IP 또는 도메인]:[포트]
AWS_SPRING_URL=http://your-aws-spring-server.com:8080

# ===========================================
# API Keys (실제 키로 교체하세요)
# ===========================================
OPENAI_API_KEY=your-openai-api-key-here
NAVER_CLIENT_ID=your-naver-client-id-here
NAVER_CLIENT_SECRET=your-naver-client-secret-here

# ===========================================
# 사용 방법
# ===========================================
# 1. 이 파일을 복사하여 .env 파일 생성: cp .env_example .env
# 2. .env 파일에서 실제 값으로 수정
# 3. 서버 실행:
#    - 로컬: ./scripts/start.sh local
#    - AWS:  ./scripts/start.sh aws
#    - 또는 APP_ENV 값에 따라: ./scripts/start.sh
